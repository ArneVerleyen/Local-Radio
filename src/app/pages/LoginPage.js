import React from 'react';

import {Login, Header, Footer} from '../components';

const LoginPage = () => {

    return (
        <div>
            <Header/>
            <Login/>
            <Footer/>
        </div>
    );
}

export default LoginPage;